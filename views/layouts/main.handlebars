<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fira+Sans+Condensed&display=swap" rel="stylesheet">
  <title>FitnessGram</title>
  <link rel="icon" type="images/x-icon" href="https://em-content.zobj.net/thumbs/120/apple/325/person-biking_1f6b4.png" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  {{!-- <link rel="stylesheet" type="text/css" href="/css/jass.css"> --}}
  <link rel="stylesheet" type="text/css" href="/css//style.css">
</head>
<body> 
    <nav class="navbar sticky-top navbar-light bg-light">
      <!-- conditionally renders login or logout links -->
        {{#if loggedIn}}
          <a class="navbar-brand" href="/">Home</a>
          <a href="/profile" class="navbar-brand">My Profile</a>
          <button class="btn btn-outline-dark" type="button" id="logout">Logout</button>
        {{else}}
          <div class="input-group">
            <div class="input-group-btn">
                <a href="/login"><button class="btn login-btn btn-outline-dark" type="button" id="logout">Login</button></a>
            </div>
          </div>
        {{/if}}
    </nav>

    <main class="container container-fluid mt-5">
    <!-- render sub layout -->
      {{{body}}}
    </main>
    <footer>

    </footer>
    <!-- Render script for logged in users only -->
    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>